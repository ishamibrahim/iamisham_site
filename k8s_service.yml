kind: Service
apiVersion: v1
metadata:
  name: imisham-service
  labels:
    type: webapp
spec:
  selector:
    type: webapp
  ports: 
    - name: http
      port: 8008
      targetPort: 8000
      nodePort: 31201
  type: LoadBalancer

